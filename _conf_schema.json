{
  "_steal_section": {
    "description": "=== 偷图设置 ===",
    "type": "text",
    "invisible": true
  },
  "steal_meme": {
    "description": "开启表情包偷取功能",
    "type": "bool",
    "default": false,
    "hint": "开启后会监听并保存聊天中的图片。需确保已配置视觉模型，否则无法分类入库。"
  },
  "steal_mode": {
    "description": "偷图模式",
    "type": "string",
    "default": "probability",
    "options": [
      "probability",
      "cooldown"
    ],
    "labels": [
      "概率模式",
      "冷却模式"
    ],
    "hint": "概率模式：每次收到图片时按设定概率决定是否偷取。冷却模式：两次偷取之间必须间隔指定秒数。"
  },
  "steal_chance": {
    "description": "偷图概率",
    "type": "float",
    "default": 0.3,
    "slider": {
      "min": 0.0,
      "max": 1.0,
      "step": 0.1
    },
    "hint": "设为 0 时从不偷取，设为 1 时每次都偷取。建议 0.2~0.5。仅概率模式生效。"
  },
  "image_processing_cooldown": {
    "description": "偷图冷却时间（秒）",
    "type": "int",
    "default": 30,
    "slider": {
      "min": 0,
      "max": 600,
      "step": 5
    },
    "hint": "仅在偷图模式为「冷却模式」时生效：两次偷取之间至少间隔这么多秒。"
  },
  "content_filtration": {
    "description": "内容审核",
    "type": "bool",
    "default": false,
    "hint": "开启后使用 VLM 审核图片内容，过滤不当图片。开启后会增加单张图片的处理时间。"
  },
  "content_filtration_fail_open": {
    "description": "审核异常时放行",
    "type": "bool",
    "default": false,
    "hint": "开启后，若内容审核本身报错（模型超时、额度用尽等），图片按「通过」处理。关闭时（默认）审核失败即拒收，更安全。"
  },
  "steal_pool_capacity": {
    "description": "待审核池容量上限",
    "type": "int",
    "default": 200,
    "slider": {
      "min": 10,
      "max": 2000,
      "step": 10
    },
    "hint": "自动偷取的图片先进「待审核池」。达到此数量后暂停自动偷取，待人工在 WebUI 审核区处理（通过入库或删除）腾出空间后自动恢复。调整后即时生效，无需重启。"
  },
  "audit_required": {
    "description": "自动偷取需人工审核",
    "type": "bool",
    "default": true,
    "hint": "开启后自动偷取的图片进入「待审核池」，需在 WebUI 审核区人工通过后才正式入库。关闭后跳过审核，自动偷取的图片直接入库分类，请配合内容审核开关使用以避免低质量图片污染图库。"
  },
  "qqofficial_steal_mode": {
    "description": "表情收录模式（QQ_Official）",
    "type": "string",
    "default": "cdn_only",
    "options": [
      "all_images",
      "cdn_only",
      "gif_only"
    ],
    "labels": [
      "所有图片按表情收录",
      "仅收录表情 CDN 特征",
      "仅收录 GIF 格式"
    ],
    "hint": "QQ_Official 平台的大表情/表情包是普通图片附件，无 OneBot 的 sub_type 标记。默认仅收录带表情 CDN 特征的图片（如商城表情）；可选全部收录，或仅收录 GIF。"
  },
  "llm_steal_param_mode": {
    "description": "LLM 偷图参数采纳策略",
    "type": "string",
    "default": "merge",
    "options": [
      "merge",
      "llm_first",
      "vlm_only"
    ],
    "labels": [
      "合并（推荐）",
      "优先 LLM 参数",
      "只用视觉分析"
    ],
    "hint": "当人格 LLM 调用偷图工具并顺带给出作品、角色、动作等参数时如何处理。合并：LLM 给的字段直接采用，没给的字段交给视觉模型补齐（推荐）。优先 LLM 参数：只要 LLM 给出了作品或角色，就完全跳过视觉模型，省一次调用、速度最快。只用视觉分析：忽略 LLM 传参，行为与旧版一致。"
  },
  "_send_section": {
    "description": "=== 发送设置 ===",
    "type": "text",
    "invisible": true
  },
  "auto_send_meme": {
    "description": "自动随聊发送表情包",
    "type": "bool",
    "default": true,
    "hint": "开启后 Bot 会在聊天中自动发送表情包。"
  },
  "auto_meme_intent_gate": {
    "description": "启用发送意图判断",
    "type": "bool",
    "default": true,
    "hint": "开启后先判断这句话适不适合配表情包，明显不适合时直接跳过，能显著减少答非所问的乱发。关闭则只看发送概率。"
  },
  "auto_meme_cancel_on_new_message": {
    "description": "新消息取消待发送表情",
    "type": "bool",
    "default": true,
    "hint": "开启后同一会话有新消息到达时，会取消上一轮仍在延迟等待中的自动表情，避免旧回复配新图。"
  },
  "meme_chance": {
    "description": "表情包发送概率",
    "type": "float",
    "default": 0.2,
    "slider": {
      "min": 0.0,
      "max": 1.0,
      "step": 0.1
    },
    "hint": "设为 0 时从不发送，设为 1 时每次都发送。建议 0.1~0.3，避免刷屏。"
  },
  "send_meme_as_gif": {
    "description": "以 GIF 格式发送",
    "type": "bool",
    "default": false,
    "hint": "开启后始终以 GIF 输出，更接近真表情包样式。关闭时按原文件格式发送（png/jpg/gif 等）。开启后高频发送大图/动图场景会有较高瞬时内存占用。"
  },
  "meme_send_delay": {
    "description": "固定延迟（秒）",
    "type": "float",
    "default": 5.0,
    "hint": "等待此时间后发送表情包，避免与分段插件冲突。建议 5.0 秒以上，让分段先发完。设为 0 则立即发送。"
  },
  "meme_send_delay_random": {
    "description": "使用随机延迟",
    "type": "bool",
    "default": false,
    "hint": "开启后在「固定延迟」和「最大随机延迟」之间随机取值，看起来更像真人。关闭则始终使用固定延迟。"
  },
  "meme_send_delay_max": {
    "description": "最大随机延迟（秒）",
    "type": "float",
    "default": 8.0,
    "slider": {
      "min": 0.0,
      "max": 60.0,
      "step": 0.5
    },
    "hint": "仅在开启随机延迟时生效，需大于固定延迟才有意义。"
  },
  "meme_send_char_delay": {
    "description": "每字追加延迟（秒）",
    "type": "float",
    "default": 0.3,
    "slider": {
      "min": 0.0,
      "max": 2.0,
      "step": 0.05
    },
    "hint": "按机器人这条回复的字数追加等待时间，模拟「先看完文字再发表情」。设为 0 则不追加。"
  },
  "smart_meme_selection": {
    "description": "智能表情包选择",
    "type": "bool",
    "default": true,
    "hint": "开启后使用综合评分（多样性 + 匹配度 + 相似度）选出最合适的表情包。关闭则随机选择（仍会避免短期重复）。"
  },
  "_emotion_section": {
    "description": "=== 情绪识别 ===",
    "type": "text",
    "invisible": true
  },
  "enable_natural_emotion_analysis": {
    "description": "智能提取检索词",
    "type": "bool",
    "default": true,
    "hint": "开启后用轻量模型从 AI 回复中提取检索词和情绪先验，增强表情包匹配；是否发送仍由概率/冷却等原有门控决定。关闭后直接用 AI 回复原文检索。"
  },
  "emotion_analysis_provider_id": {
    "description": "情绪分析专用模型",
    "type": "string",
    "hint": "留空则使用当前会话模型。小参数量模型即可胜任，建议使用轻量免费模型以降低延迟和消耗。",
    "_special": "select_provider",
    "default": ""
  },
  "emotion_analysis_prompt": {
    "description": "情绪分析提示词",
    "type": "text",
    "default": "你是表情包检索词提取器。根据对话从 AI 回复中提取能搜到表情包的关键词或短句，并给出 1~3 个情绪先验。\n可选情绪分类：{emotion_list}\n\n任务：\n1. 只从回复里摘录关键词或短句，不要总结成抽象概念。\n2. 如果没有明显关键词，就用回复原文。\n3. emotions 从给定分类里选 1~3 个，按相关度排序。每个分类机会均等，不要把某一类当默认。\n\n用户消息：{user_message}\n回复：{llm_reply}\n\n输出示例：\n{\"query\": \"摸鱼 下班 辛苦了\", \"emotions\": [\"tired\", \"sigh\"]}\n",
    "hint": "生成检索词和 1~3 个情绪先验。支持占位符：{emotion_list}、{llm_reply}、{user_message}。为空时使用默认提示词。兼容旧的单分类名输出。"
  },
  "_model_section": {
    "description": "=== 模型配置 ===",
    "type": "text",
    "invisible": true
  },
  "vision_provider_id": {
    "description": "视觉模型",
    "type": "string",
    "hint": "用于图片分类。留空则自动使用 AstrBot 全局配置的图片描述模型（default_image_caption_provider_id）。",
    "_special": "select_provider"
  },
  "enable_embedding_search": {
    "description": "启用嵌入向量检索",
    "type": "bool",
    "default": true,
    "hint": "开启后用远程文本嵌入按语义召回（图上文字 + 使用场景 + 描述）。关闭则只用关键词（BM25）。不使用本地 CLIP。未配置嵌入模型时自动降级为 BM25。"
  },
  "embedding_provider_id": {
    "description": "嵌入模型 ID",
    "type": "string",
    "default": "",
    "hint": "用于生成表情包语义向量的嵌入模型。留空则自动使用 AstrBot 首个可用的 Embedding Provider。在 AstrBot 管理后台 → LLM 配置中可查看已配置的 Embedding 模型 ID（通常以 _embedding 结尾，如 openai_embedding、qwen_embedding 等）。此字段填写的是 Embedding 模型 ID，不是聊天模型或视觉模型 ID。"
  },
  "_batch_section": {
    "description": "=== 批量识别（WebUI 批量导入）===",
    "type": "text",
    "invisible": true
  },
  "batch_analyze_concurrency": {
    "description": "批量识别并发数",
    "type": "int",
    "default": 2,
    "slider": {
      "min": 1,
      "max": 16,
      "step": 1
    },
    "hint": "同时送进视觉模型的图片数量。官方 API 建议 2~4；本地部署可调高。调太高容易触发上游 429 限流。"
  },
  "batch_analyze_rpm": {
    "description": "批量识别每分钟请求上限",
    "type": "int",
    "default": 20,
    "slider": {
      "min": 0,
      "max": 600,
      "step": 5
    },
    "hint": "每分钟最多发起多少次视觉分析请求，用于避开上游速率限制。设为 0 表示不限速，只受并发数约束。不清楚上游额度时，先按官方文档的 RPM 打七折填。"
  },
  "batch_analyze_max_retries": {
    "description": "批量识别重试次数",
    "type": "int",
    "default": 3,
    "slider": {
      "min": 0,
      "max": 8,
      "step": 1
    },
    "hint": "单张图片遇到 429、超时、5xx 等可恢复错误时的重试次数。参数错误、鉴权失败等不可恢复错误不会重试。"
  },
  "batch_analyze_retry_backoff": {
    "description": "批量识别重试退避基数（秒）",
    "type": "float",
    "default": 2.0,
    "slider": {
      "min": 1.0,
      "max": 10.0,
      "step": 0.5
    },
    "hint": "重试等待时间按此基数指数增长（第 n 次约等待 基数^n 秒，含随机抖动，上限 60 秒）。上游返回 Retry-After 时优先听上游。"
  },
  "_group_section": {
    "description": "=== 群聊过滤 ===",
    "type": "text",
    "invisible": true
  },
  "send_target_whitelist": {
    "description": "发表情白名单",
    "type": "list",
    "default": [],
    "hint": "格式：group:群号 或 user:QQ号。非空时仅白名单内的目标允许自动发送表情包。"
  },
  "send_target_blacklist": {
    "description": "发表情黑名单",
    "type": "list",
    "default": [],
    "hint": "格式：group:群号 或 user:QQ号。可与白名单同时生效，冲突时按下方优先级规则判定。"
  },
  "send_target_filter_mode": {
    "description": "发表情名单优先级",
    "type": "string",
    "default": "whitelist_first",
    "options": [
      "whitelist_first",
      "blacklist_first"
    ],
    "labels": [
      "白名单优先",
      "黑名单优先"
    ],
    "hint": "白名单优先：白名单命中时允许，否则再查黑名单。黑名单优先：黑名单命中时拒绝，否则再查白名单。"
  },
  "steal_target_whitelist": {
    "description": "偷表情白名单",
    "type": "list",
    "default": [],
    "hint": "格式：group:群号 或 user:QQ号。非空时仅白名单内的目标允许自动偷取。"
  },
  "steal_target_blacklist": {
    "description": "偷表情黑名单",
    "type": "list",
    "default": [],
    "hint": "格式：group:群号 或 user:QQ号。可与白名单同时生效，冲突时按下方优先级规则判定。"
  },
  "steal_target_filter_mode": {
    "description": "偷表情名单优先级",
    "type": "string",
    "default": "whitelist_first",
    "options": [
      "whitelist_first",
      "blacklist_first"
    ],
    "labels": [
      "白名单优先",
      "黑名单优先"
    ],
    "hint": "白名单优先：白名单命中时允许，否则再查黑名单。黑名单优先：黑名单命中时拒绝，否则再查白名单。"
  },
  "_storage_section": {
    "description": "=== 存储及进阶管理 ===",
    "type": "text",
    "invisible": true
  },
  "max_reg_num": {
    "description": "最大表情包数量",
    "type": "int",
    "default": 100,
    "slider": {
      "min": 10,
      "max": 2000,
      "step": 10
    },
    "hint": "表情包存储上限。达到上限后自动按清理策略淘汰旧表情包。"
  },
  "storage_cleanup_strategy": {
    "description": "存储清理策略",
    "type": "string",
    "default": "balanced",
    "options": [
      "conservative",
      "balanced",
      "aggressive"
    ],
    "labels": [
      "保守",
      "均衡（推荐）",
      "激进"
    ],
    "hint": "保守：只清理失效索引和临时文件。均衡：额外清理孤立文件和缩略图缓存。激进：连原图备份（raw 目录）一起清，最省空间但无法再回溯原图。"
  },
  "custom_meme_classification_prompt": {
    "description": "VLM 分类提示词",
    "type": "text",
    "default": "<role>\n你是中文互联网表情包标注专家。\n</role>\n\n<task>\n给这张表情包写检索档案。分类只是浏览分区，重点是图上文字和画面本身。\n</task>\n\n<categories>\n{emotion_list}\n</categories>\n\n<classification_guide>\n只按画面选择一个最贴合的分类，每个分类机会均等，不要把某一类当默认。\n- happy: 开心、愉悦\n- sad: 难过、失落\n- angry: 生气、恼火\n- shy: 害羞、腼腆\n- surprised: 惊讶、意外\n- troll: 整活、搞怪\n- cry: 哭、委屈\n- confused: 困惑、疑惑\n- embarrassed: 尴尬、窘迫\n- love: 喜欢、心动\n- disgust: 嫌弃、厌恶\n- fear: 害怕、紧张\n- excitement: 兴奋、激动\n- tired: 困倦、疲惫\n- sigh: 无奈、叹气\n- thank: 感谢、道谢\n- dumb: 无语、傻眼\n</classification_guide>\n\n<important>\n1. overlay_text 必须逐字抄出图上可见的中文或英文；没有文字则输出空字符串。这是检索最重要的字段。\n2. description 写画面主体，40字以内，不要只重复分类名。\n3. scenes 只写图里能看见的信息，1~2 个短词或短语，优先复述 overlay_text。不要编造发图时的对话情境。\n4. tags 给 0~3 个画面关键词，没有就输出空数组。不要为了凑数硬编标签。\n5. emotions 给 1~3 个相关分类，必须来自给定分类，第一个应与 category 一致。\n</important>\n\n<output_format>\n只输出JSON，不要有任何其他文字。格式如下：\n{\"category\": \"分类名称\", \"emotions\": [\"分类1\"], \"tags\": [], \"description\": \"画面描述\", \"overlay_text\": \"图上原文\", \"scenes\": [\"短语1\"]}\noverlay_text 为图上原文，没有则 \"\"；tags 没有则 []。\n</output_format>\n\n<example>\n{\"category\": \"confused\", \"emotions\": [\"confused\"], \"tags\": [], \"description\": \"卡通脸瞪大眼睛\", \"overlay_text\": \"啊？\", \"scenes\": [\"啊？\"]}\n</example>",
    "hint": "支持占位符：{emotion_list}（分类列表）。为空时使用默认提示词。"
  },
  "custom_meme_classification_with_filter_prompt": {
    "description": "VLM 分类提示词（带审核）",
    "type": "text",
    "default": "<role>\n你是中文互联网表情包审核与标注专家。\n</role>\n\n<task>\n第一步审核内容是否合规，第二步写检索档案。分类只是浏览分区，重点是图上文字和画面本身。\n</task>\n\n<content_filter>\n裸露色情、暴力血腥、政治敏感、广告营销时，只输出：{\"approved\": false, \"reason\": \"审核不通过\"}\n</content_filter>\n\n<categories>\n{emotion_list}\n</categories>\n\n<classification_guide>\n只按画面选择一个最贴合的分类，每个分类机会均等，不要把某一类当默认。\n- happy: 开心、愉悦\n- sad: 难过、失落\n- angry: 生气、恼火\n- shy: 害羞、腼腆\n- surprised: 惊讶、意外\n- troll: 整活、搞怪\n- cry: 哭、委屈\n- confused: 困惑、疑惑\n- embarrassed: 尴尬、窘迫\n- love: 喜欢、心动\n- disgust: 嫌弃、厌恶\n- fear: 害怕、紧张\n- excitement: 兴奋、激动\n- tired: 困倦、疲惫\n- sigh: 无奈、叹气\n- thank: 感谢、道谢\n- dumb: 无语、傻眼\n</classification_guide>\n\n<important>\n1. overlay_text 必须逐字抄出图上可见的中文或英文；没有文字则输出空字符串。这是检索最重要的字段。\n2. description 写画面主体，40字以内，不要只重复分类名。\n3. scenes 只写图里能看见的信息，1~2 个短词或短语，优先复述 overlay_text。不要编造发图时的对话情境。\n4. tags 给 0~3 个画面关键词，没有就输出空数组。不要为了凑数硬编标签。\n5. emotions 给 1~3 个相关分类，必须来自给定分类，第一个应与 category 一致。\n6. 无论审核是否通过，都只能输出一个 JSON 对象，不要添加“审核通过：”“结果：”或 markdown 代码块。\n</important>\n\n<output_format>\n只输出 JSON，不要有任何其他文字。格式如下：\n审核通过：{\"approved\": true, \"category\": \"分类名称\", \"emotions\": [\"分类1\"], \"tags\": [], \"description\": \"画面描述\", \"overlay_text\": \"图上原文\", \"scenes\": [\"短语1\"]}\n审核不通过：{\"approved\": false, \"reason\": \"审核不通过\"}\n请注意，上面两行只是说明审核通过和不通过时各自应该输出的 JSON 结构，实际回复时不要输出“审核通过：”或“审核不通过：”这几个字。\noverlay_text 为图上原文，没有则 \"\"；tags 没有则 []。\n</output_format>\n\n<example>\n{\"approved\": true, \"category\": \"confused\", \"emotions\": [\"confused\"], \"tags\": [], \"description\": \"卡通脸瞪大眼睛\", \"overlay_text\": \"啊？\", \"scenes\": [\"啊？\"]}\n</example>",
    "hint": "开启内容审核时使用的 VLM 分类提示词。支持占位符：{emotion_list}（分类列表）。为空时使用默认提示词。"
  },
  "_webui_section": {
    "description": "=== WebUI ===",
    "type": "text",
    "invisible": true
  },
  "webui_theme": {
    "description": "WebUI 默认主题",
    "type": "string",
    "default": "auto",
    "options": [
      "auto",
      "dark",
      "light",
      "pixel",
      "terminal"
    ],
    "labels": [
      "跟随宿主",
      "暗色鎏金",
      "亮色",
      "像素",
      "终端"
    ],
    "hint": "页面尚未保存主题选择时使用该默认值；在「表情管理」中切换后，将持续使用页面保存的主题。"
  },
  "_smart_section": {
    "description": "=== 智能选择：文字距离融合权重预设 ===",
    "type": "text",
    "invisible": true
  },
  "sim_weight_preset": {
    "description": "文字距离融合预设",
    "type": "string",
    "default": "balanced",
    "options": [
      "balanced",
      "keyword",
      "semantic",
      "strict"
    ],
    "labels": [
      "均衡",
      "关键词优先",
      "语义优先",
      "严格匹配"
    ],
    "hint": "预设决定多个相似度算法的融合权重：均衡（默认）、关键词优先（子串/字符重叠更敏感）、语义优先（n-gram/余弦更重）、严格匹配（编辑距离和字符级更重，减少误发）。"
  }
}
